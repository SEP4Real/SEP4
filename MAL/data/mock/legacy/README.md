# Legacy Mock Data

This folder contains the old synthetic CSVs and generators. They are preserved for reference, but the real dataset import and merge workflow should use `data/raw/`, `data/interim/`, and `data/processed/`.

