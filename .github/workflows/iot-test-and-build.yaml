name: IoT CI/CD

on:
  pull_request:
    branches:
      - main
      - dev
  workflow_dispatch:

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      iot: ${{ steps.filter.outputs.iot }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check for IoT changes
        uses: dorny/paths-filter@v3
        id: filter
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          base: ${{ github.base_ref }}
          filters: |
            iot:
              - 'IOT/**'

  iot-test:
    needs: detect-changes
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: IOT
    steps:
      - uses: actions/checkout@v4

      - name: Skip IoT tests (no IoT changes)
        if: needs.detect-changes.outputs.iot != 'true'
        run: echo "No IoT changes detected; skipping tests."

      - name: Install lcov
        if: needs.detect-changes.outputs.iot == 'true'
        run: sudo apt-get install -y lcov

      - name: Create secrets.ini
        if: needs.detect-changes.outputs.iot == 'true'
        run: |
          cat > secrets.ini << EOF
          [secrets]
          build_flags =
              -DSERVER_HOST='"test_host"'
              -DSERVER_PORT=2137
              -DWIFI_SSID='"test_ssid"'
              -DWIFI_PASSWORD='"test_password"'
          EOF

      - name: Run tests with coverage
        if: needs.detect-changes.outputs.iot == 'true'
        run: make coverage

      - name: Upload HTML report as artifact
        if: needs.detect-changes.outputs.iot == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: IOT/coverage-html

  iot-build:
    needs: [detect-changes, iot-test]
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: IOT
    steps:
      - uses: actions/checkout@v4

      - name: Skip IoT build (no IoT changes)
        if: needs.detect-changes.outputs.iot != 'true'
        run: echo "No IoT changes detected; skipping build."

      - name: Cache PlatformIO
        if: needs.detect-changes.outputs.iot == 'true'
        uses: actions/cache@v4
        with:
          path: ~/.platformio
          key: ${{ runner.os }}-pio-${{ hashFiles('IOT/platformio.ini') }}

      - name: Install PlatformIO
        if: needs.detect-changes.outputs.iot == 'true'
        run: pip install platformio

      - name: Create secrets.ini
        if: needs.detect-changes.outputs.iot == 'true'
        run: |
          cat > secrets.ini << EOF
          [secrets]
          build_flags =
              -DSERVER_HOST='"test_host"'
              -DSERVER_PORT=2137
              -DWIFI_SSID='"test_ssid"'
              -DWIFI_PASSWORD='"test_password"'
          EOF

      - name: Build
        if: needs.detect-changes.outputs.iot == 'true'
        run: pio run -e megaatmega2560

      - name: Upload .hex artifact
        if: needs.detect-changes.outputs.iot == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: firmware
          path: IOT/.pio/build/megaatmega2560/firmware.hex