name: IoT CI/CD

on:
  pull_request:
    branches:
      - main
      - dev

jobs:
  iot-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: IOT
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check for IoT changes
        uses: dorny/paths-filter@v3
        id: changes
        with:
          base: ${{ github.base_ref }}
          filters: |
            iot:
              - 'IOT/**'

      - name: Install lcov
        if: steps.changes.outputs.iot == 'true'
        run: sudo apt-get install -y lcov

      - name: Create secrets.ini
        if: steps.changes.outputs.iot == 'true'
        run: |
          cat > secrets.ini << EOF
          [secrets]
          build_flags =
              -DSERVER_HOST='"test_host"'
              -DSERVER_PORT=2137
              -DWIFI_SSID='"test_ssid"'
              -DWIFI_PASSWORD='"test_password"'
          EOF

      - name: Run tests with coverage
        if: steps.changes.outputs.iot == 'true'
        run: make coverage

      - name: Upload HTML report as artifact
        if: steps.changes.outputs.iot == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: IOT/coverage-html

  iot-build:
    runs-on: ubuntu-latest
    needs: iot-test
    defaults:
      run:
        working-directory: IOT
    steps:
      - uses: actions/checkout@v4

      - name: Check for IoT changes
        uses: dorny/paths-filter@v3
        id: changes
        with:
          filters: |
            iot:
              - 'IOT/**'

      - name: Cache PlatformIO
        if: steps.changes.outputs.iot == 'true'
        uses: actions/cache@v4
        with:
          path: ~/.platformio
          key: ${{ runner.os }}-pio-${{ hashFiles('IOT/platformio.ini') }}

      - name: Install PlatformIO
        if: steps.changes.outputs.iot == 'true'
        run: pip install platformio

      - name: Create secrets.ini
        if: steps.changes.outputs.iot == 'true'
        run: |
          cat > secrets.ini << EOF
          [secrets]
          build_flags =
              -DSERVER_HOST='"test_host"'
              -DSERVER_PORT=2137
              -DWIFI_SSID='"test_ssid"'
              -DWIFI_PASSWORD='"test_password"'
          EOF

      - name: Build
        if: steps.changes.outputs.iot == 'true'
        run: pio run -e megaatmega2560

      - name: Upload .hex artifact
        if: steps.changes.outputs.iot == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: firmware
          path: IOT/.pio/build/megaatmega2560/firmware.hex